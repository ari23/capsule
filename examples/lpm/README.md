# LPM daemon example

[Lpm](https://en.wikipedia.org/wiki/Longest_prefix_match) is an algorithm used by routers to select an entry from forwarding table.

## Running the application

The example is located in the `examples/lpm` sub-directory. To run the application,

```
/examples/lpm$ cargo run -- -f lpm.toml
```